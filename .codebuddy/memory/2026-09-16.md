# 2026-09-16

## VSCode 工作区配置提交

- 新增 `.vscode/settings.json`（`workbench.tree.indent: 16`、参考线关闭、`explorer.compactFolders: false`、平滑滚动），按 regular-commit skill 流程以 `chore(vscode)/...` 提交（b059f66）。
- 起因：用户问 VSCode 文件树缩进调整，随后把配置落成工作区设置并提交。

## go-code-spec.md 新增 err 复用规则

- 应用户要求在 `assets/rulehub/go-code-spec.md` 新增「error 变量声明一次，后续用 = 复用」一节：函数内 err 已声明则不再 `:=` 重新声明，判错用 `if err = xxx; err != nil`；新返回值在 var 块预声明后 `xxx, err = ...` 赋值。
