/**
 * Re-export from types/ directory for backward compatibility.
 * New imports should use: `import { ... } from './types'`
 * (which resolves to types/index.ts automatically)
 */
export * from './types/index';
