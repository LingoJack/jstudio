# 2026-07-11 工作日志

## 文件结构优化分析 → docs/file-structure-optimization-plan.md
- 分析了 `src/` 201 个 ts/tsx 文件（~44.6k 行）的实际结构，写出可执行优化计划文档到 `docs/file-structure-optimization-plan.md`
- 关键发现（均经实测）：
  - **确认死文件**：`components/editor/SearchBar.tsx`、`components/editor/hooks/useSizeMigration.ts`（全仓 0 引用）
  - **疑似死**：`lib/editor/content/htmlToRichText.ts`、`lib/editor/content/richTextToHtml.ts`（0 调用方，待 grep 确认）
  - **非死（扫描盲区）**：`TableSizeSelector.tsx`（动态 `import()`）、`globalShortcutActions.ts`（副作用 `import './...'`）
  - **巨型文件**：GraphCanvas.tsx 1468、EditorCursorTrail.ts 1462、i18n.ts 1331、DocumentList.tsx 1104、BlockEditor.tsx 1064
  - **边界模糊**：`lib/editor`(58) 与 `components/editor`(38) 分散；lib 含 4 个 .tsx。但 slashMenu 的 .tsx 被 lib 侧渲染，**不应搬到 components**（会反向依赖）
  - `AGENTS.md` 已与真实结构脱节（缺 lib/constants、commandPalette、agent、ime、graph、hooks 等）
- 方法论：自写相对导入图扫描（resolve 归一到相对路径、排除 barrel）找 orphan；但动态/副作用 import 需人工 grep 复核。已建议引入 knip 做防回归。
- 推荐：增量清理（删死代码+拆巨型文件+文档化边界+knip），暂缓 feature-sliced 大重构。

## 文档 v2 细化（同日下午）
- 用户要求"更具体、可执行"。重写了 `docs/file-structure-optimization-plan.md` 为 v2：
  - Phase 0/1/2/5 改成**可直接复制运行的 shell 脚本**（装 knip、写 knip.json 全文、git rm 死文件带验证 grep、main.tsx 去扩展名、docs/architecture.md 边界规则全文）
  - Phase 3 每个巨型文件给出**精确行号提取表**（提取内容/行号/目标文件/源文件替换为的一行 import）+ 新文件代码桩
  - 实测锚点：i18n.ts translations=22–1305；SectionedBlockEditor 分段逻辑 43–81；GraphCanvas ShapeGlyph=161–288、styleForShape=289–340、交互 handler 在组件体 423–920；BlockEditor 各 useCallback 行号（handleChange 184、flushPendingEdits 228、trail setup 647）；DocumentList return=816、<DocumentContextMenu>=1019
  - 新增"一键执行脚本"附录 A、风险回滚附录 B、决策点附录 C
