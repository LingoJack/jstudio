---
id: req-YYYY-MMDD-slug
title: 一句话需求名，动词开头
owner: 负责人
created: YYYY-MM-DD
updated: YYYY-MM-DD
status: developing
---

<!--
status: developing（在做） / delivered（已交付） / dropped（不做了，正文说明原因）
文件名与 frontmatter 的 id 一致，放在 docs/apis/reqs/ 下。
下面每一节的注释是填写提示，填完删掉注释。
-->

# 一句话需求名

## 需求背景

<!--
现在什么场景走不通、谁在等它、不做会怎样。写具体场景，不写"为了完善功能""提升体验"。
例：桌面端文档只存本地，换机器或重装就丢；用户已经反馈过两次数据找不回来。
-->

## 预期表现

<!--
做完之后，从调用方视角能观察到什么。逐条写，每条都是可验证的现象：
- 谁在什么时候调什么，得到什么
- 数据发生什么变化（新增/覆盖/追加）
- 重复调用、并发调用、越界输入分别怎么表现
不写内部实现（哪张表、哪个函数）。
-->

1.
2.

## 范围

<!-- 这次做什么、明确不做什么。"不做"这一半同样重要，它挡住后续扯皮。 -->

- 做：
- 不做：

## 接口清单

<!--
契约定稿后填。每行一个接口，id 对应 contract.jsonl 里的条目，状态与条目保持一致。
详细入参出参不写在这里，看 docs/apis/API.md。
-->

| 条目 id | 接口 | 状态 |
|---------|------|------|
| | `METHOD /api/v1/...` | draft |

## 数据与配置

<!--
需要新建/改表吗（改动进 backend/schema.sql，由运维手动执行）？
需要新增配置项吗（配置可被 JS_ 前缀环境变量覆盖）？
需要新的对象存储 bucket 吗？没有就写"无"。
-->

## 验收

<!--
怎么算做完。每条都要能被别人独立执行验证，写成可操作的检查步骤。
例：登录后 PUT 两次同一 docId，GET snapshots 返回 total=2 且 revision 为 2、1。
-->

1.
2.

## 变更记录

<!--
需求或契约每次实质变更追加一行。破坏性变更（删字段、改语义、改错误码）要写清对调用方的影响，
以及 desktop / miniprogram 两端是否已同步改完。
-->

| 日期 | 变更 | 影响 |
|------|------|------|
| YYYY-MM-DD | 需求建立 | - |
