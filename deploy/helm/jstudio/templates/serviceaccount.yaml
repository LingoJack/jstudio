{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "jstudio.serviceAccountName" . }}
  labels:
    {{- include "jstudio.labels" . | nindent 4 }}
{{- end }}
